# Database (must match backend's database)
DATABASE_URL="postgresql://postgres@localhost:5433/cinetrack"

# Artifact directory (defaults to this file's directory)
FILMMOSAIC_ARTIFACT_DIR=.

# Server
HOST=0.0.0.0
PORT=8000
